(typeof DFine === 'function' ? {DFine : DFine} : require('DFine')._(require,exports).DFine);

DFine('kadoh.peer', ['kadoh.core.class', 'kadoh.kbucket'], function(class, kbucket) {
  var exports;
  
  exports = class({
      initialize : 
    
    , 
    
  });
  
  return exports ;
  
});